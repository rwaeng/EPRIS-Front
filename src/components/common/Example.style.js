import styled from 'styled-components';
import { flexCenter } from '../../styles/common';
import { bold56 } from '../../styles/font';

const S = {};

S.Wrapper = styled.span`
  ${flexCenter}
  ${bold56}
  color: var(--white);
`;

export { S };
